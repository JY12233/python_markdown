# 文件操作介绍

## 定义与本质

Python 中的文件操作，是指通过 Python 解释器调用操作系统底层的 I/O（输入/输出）接口，以编程方式完成对文本文件（如 `.txt`、`.csv`、`.json`）和二进制文件（如 `.png`、`.jpg`、`.pickle`）的创建、打开、读取、写入、追加、定位、关闭及异常容错等系统级操作。其核心在于以编程方式在内存与外部存储介质（如硬盘、SSD）之间建立可控的数据通道，实现数据的持久化存储与读取。

## 核心作用

文件操作是 Python 编程中最基础且关键的系统级交互能力，其主要作用体现在以下三个维度：

- 数据持久化

  将程序运行过程中产生的数据（如计算结果、用户信息、模型权重）永久保存到磁盘，防止程序退出后数据丢失。

- 配置与资源管理

  读取外部配置文件（如 `.ini`、`.yaml`）、日志记录、静态资源加载等，实现代码与数据的分离。

- 数据处理与自动化

  批量处理文本文件、解析 CSV/JSON 数据、自动化脚本开发等，大幅提升数据处理效率。

## 标准流程

Python 文件操作严格遵循“打开 -> 读写 -> 关闭”的三段式范式。

### 打开文件

`open()` 是文件操作的起点，用于向操作系统请求一个文件描述符，并返回一个文件对象（文件句柄）。

```python
open(
    file,  # 必填，文件路径（字符串或路径对象）
    mode='r',  # 可选，打开模式（默认为只读文本模式）
    buffering=-1,  # 可选，缓冲策略（-1 为系统默认，0 为无缓冲，1 为行缓冲，>1 为指定缓冲区大小）
    encoding=None,  # 可选，文本模式下的字符编码（强烈建议显式指定为 'utf-8'）
    errors=None,  # 可选，编码/解码错误的处理策略（如 'strict'、'ignore'、'replace'）
    newline=None,  # 可选，控制换行符的处理方式（影响跨平台兼容性）
    closefd=True,  # 可选，关闭文件时是否同时关闭底层文件描述符
    opener=None  # 可选，自定义的文件打开器
)
```

### `mode` 参数

文件打开的模式。

| 模式  | 说明                                                         |
| ----- | ------------------------------------------------------------ |
| `'r'` | 只读模式（默认）。文件不存在则抛出 `FileNotFoundError`       |
| `'w'` | 写入模式。文件存在则清空原内容，不存在则创建新文件           |
| `'a'` | 追加模式。写入内容自动追加到文件末尾，文件不存在则创建       |
| `'x'` | 独占创建模式。文件已存在则抛出 `FileExistsError`，防止误覆盖 |
| `'b'` | 二进制模式。需与其他模式组合使用（如 `'rb'`、`'wb'`），禁用文本转换 |
| `'t'` | 文本模式（默认）。启用换行符标准化与编码转换                 |
| `'+'` | 读写模式。允许在同一句柄上交替执行读写操作（如 `'r+'`、`'w+'`） |

### 读取文件内容

| 方法                 | 说明                                                         |
| -------------------- | ------------------------------------------------------------ |
| `read(size=-1)`      | 一次性读取全部内容（`size` 为负数或省略时）。大文件易导致内存溢出 |
| `readline(size=-1)`  | 逐行读取，保留行尾换行符 `\n`。内存友好，适合处理超长日志    |
| `readlines(hint=-1)` | 读取所有行并返回字符串列表。`hint` 可限制读取的总字节数      |

### 写入文件内容

| 方法                | 说明                                                         |
| ------------------- | ------------------------------------------------------------ |
| `write(s)`          | 将字符串 `s` 写入文件（文本模式）或字节串（二进制模式）。返回写入的字符/字节数 |
| `writelines(lines)` | 将字符串序列（如列表）写入文件。**不会自动添加换行符**，需手动拼接 `\n` |

### 文件指针定位

| 方法                     | 说明                                                         |
| ------------------------ | ------------------------------------------------------------ |
| `seek(offset, whence=0)` | 移动文件指针。`whence=0` 从文件头，`1` 从当前位置，`2` 从文件尾 |
| `tell()`                 | 返回当前文件指针的字节偏移量                                 |
| `flush()`                | 强制清空缓冲区，确保数据即时落盘                             |

### 关闭文件与上下文管理器

- 手动关闭

  使用 `file.close()` 方法。若中间发生异常，可能导致文件句柄泄漏。

- 上下文管理器（强烈推荐）

  使用 `with` 语句。无论代码块是否正常退出或发生异常，都会自动调用 `close()`，彻底规避资源泄漏风险。

```python
# 标准的文件操作范式
with open('data.txt', 'r', encoding='utf-8') as f:
    content = f.read()
# 离开 with 代码块后，文件自动关闭
```

## 注意事项

1. 编码问题

   在文本模式下，必须显式指定 `encoding` 参数（推荐 `'utf-8'`）。若不指定，Python 将依赖系统默认编码（Windows 常为 `gbk`，Linux/macOS 多为 `utf-8`），极易引发 `UnicodeDecodeError` 或中文乱码。

2. `w` 模式的破坏性

   `w` 模式会无条件清空原文件内容。若需保留原内容并追加数据，应使用 `a` 模式。

3. `writelines()` 无换行

   `writelines()` 不会自动为每一行添加换行符，开发者需在写入前手动拼接 `\n`。

4. 路径操作

   推荐使用 `pathlib` 模块或 `os.path.join()` 进行跨平台路径拼接，避免硬编码路径分隔符。

5. 异常处理

   文件操作必须配合 `try...except` 捕获 `FileNotFoundError`、`PermissionError`、`UnicodeDecodeError` 等具体异常，避免程序静默失败。

# 文件对象

`open()` 函数返回的文件对象（File Object）是 Python 中用于与底层操作系统文件进行交互的核心句柄。使用时通常配合 `with` 语句（例如 `with open('file.txt', 'r') as f:`）来确保资源的安全释放，当需要在 Python 程序中进行文件的读取、写入、追加、定位或获取文件状态信息时，就需要使用该对象。

## 创建对象时的格式

文件对象并非通过类直接实例化，而是通过内置函数 `open()` 创建。其完整参数格式如下：

```python
open(
    file,  # 必填，指定要打开的文件路径（字符串或路径对象）
    mode='r',  # 可选，指定文件的打开模式（默认为只读文本模式）
    buffering=-1,  # 可选，指定缓冲策略（-1 为系统默认，0 为无缓冲，1 为行缓冲，>1 为指定缓冲区大小）
    encoding=None,  # 可选，指定文本模式下的字符编码（强烈建议显式指定，如 'utf-8'）
    errors=None,  # 可选，指定编码或解码错误的处理策略（如 'strict'、'ignore'、'replace'）
    newline=None,  # 可选，控制换行符的转换行为（影响跨平台兼容性）
    closefd=True,  # 可选，关闭文件时是否同时关闭底层文件描述符
    opener=None  # 可选，指定自定义的文件打开器（可调用对象）
)
```

## 参数详解

| 参数名称    | 作用说明                                   | 可选值或类型说明                                             |
| ----------- | ------------------------------------------ | ------------------------------------------------------------ |
| `file`      | 指定需要操作的文件路径。                   | **字符串**、**字节串**或 **`os.PathLike`** 对象              |
| `mode`      | 指定文件的打开模式（读写、文本或二进制）。 | **字符串**：<br />`'r'`（只读）、<br />`'w'`（写入覆盖）、<br />`'a'`（追加）、<br />`'x'`（独占创建）、<br />`'b'`（二进制）、<br />`'t'`（文本）、<br />`'+'`（读写） |
| `buffering` | 控制 I/O 操作的缓冲行为。                  | **整数**：<br />`0`（无缓冲）、<br />`1`（行缓冲）、<br />`>1`（指定固定大小的缓冲区）、<br />`-1`（系统默认） |
| `encoding`  | 指定文本模式下的字符编码格式。             | **字符串**：例如 `'utf-8'`、`'gbk'`、`'ascii'`               |
| `errors`    | 指定编码或解码失败时的处理方式。           | **字符串**：<br />`'strict'`（默认，抛出异常）、<br />`'ignore'`（忽略错误）、<br />`'replace'`（替换为占位符） |
| `newline`   | 控制换行符的识别与转换。                   | **字符串**：<br />`None`（通用换行）、<br />`''`（不转换）、<br />`'\n'`、<br />`'\r'`、<br />`'\r\n'` |
| `closefd`   | 控制关闭文件对象时是否关闭底层描述符。     | **布尔值**：<br />`True`（默认）<br />`False`                |
| `opener`    | 提供自定义的文件打开逻辑。                 | **可调用对象**：接收 `(file, flags)` 并返回文件描述符        |

## 常用属性

| 属性名称   | 说明                                                    |
| ---------- | ------------------------------------------------------- |
| `name`     | 返回打开文件时传入的路径名称                            |
| `mode`     | 返回文件当前的打开模式字符串                            |
| `encoding` | 返回文本模式下的编码格式（二进制模式下为 `None`）       |
| `closed`   | 返回布尔值，表示文件是否已关闭                          |
| `newlines` | 返回读取过程中遇到的所有换行符类型（未遇到时为 `None`） |

## 常用方法

| 方法名称                 | 说明                                                         |
| ------------------------ | ------------------------------------------------------------ |
| `read(size=-1)`          | 读取并返回指定大小的内容（文本模式为字符数，二进制模式为字节数）。<br />省略参数则读取全部内容 |
| `readline(size=-1)`      | 读取并返回一行内容（包含行尾换行符）。省略参数则读取到行尾   |
| `readlines(hint=-1)`     | 读取所有行并返回一个列表。`hint` 参数可限制读取的总字节数或字符数 |
| `write(s)`               | 将字符串或字节串写入文件，返回写入的字符数或字节数           |
| `writelines(lines)`      | 将一个字符串或字节串的可迭代对象写入文件（不会自动添加换行符） |
| `seek(offset, whence=0)` | 移动文件指针。`whence=0` 从文件头，`1` 从当前位置，`2` 从文件尾 |
| `tell()`                 | 返回当前文件指针的偏移量（字节位置）                         |
| `flush()`                | 强制将缓冲区中的数据写入底层文件<br />只是 write，内容还是写在内存中的，并没有写入文件。 |
| `close()`                | 关闭文件并释放系统资源。推荐使用 `with` 语句代替手动调用     |
| `fileno()`               | 返回底层文件描述符（整数）                                   |
| `isatty()`               | 返回布尔值，判断文件是否连接到终端设备                       |
| `readable()`             | 返回布尔值，判断文件是否支持读取操作                         |
| `writable()`             | 返回布尔值，判断文件是否支持写入操作                         |
| `seekable()`             | 返回布尔值，判断文件是否支持随机访问（指针移动）             |
| `truncate(size=None)`    | 将文件大小截断至指定大小。省略参数则截断至当前指针位置       |

# 文件指针

## 定义与本质

文件指针（File Pointer），在 Python 中通常被称为文件位置标记（File Position Marker）。文件指针是一个指向文件内部特定字节位置的内部游标。当文件被打开时，操作系统会在内存中为该文件维护一个状态结构，其中包含一个偏移量变量，用于记录当前读写操作在文件中的确切位置。

## 核心作用

文件指针是顺序读写与随机访问的基础，其主要作用体现在以下三个维度：

- 维持顺序读写状态

  在默认的顺序读写模式下，每次执行读取或写入操作后，文件指针会自动向后移动相应的字节数或字符数，确保下一次操作能从正确的位置继续，避免数据被重复读取或覆盖。

- 实现随机访问（Random Access）

  通过手动控制文件指针的位置，程序可以跳过不需要的数据块，直接定位到文件的任意位置进行读取或修改，而无需从头遍历整个文件。这在处理大型二进制文件（如视频、数据库文件）或日志文件时至关重要。

- 支持文件截断与追加

  文件指针的当前位置决定了 `truncate()` 操作的截断点，以及在某些模式下 `write()` 操作的写入起点。

## 操作方法

在 Python 中，文件指针的操作主要通过文件对象的以下三个核心方法完成：

### 获取当前位置

`tell()` 方法用于返回当前文件指针的偏移量。

```python
file_object.tell()
```

返回一个整数，表示文件指针距离文件开头的偏移量。

在文本模式下（非二进制模式），由于换行符转换和编码问题，`tell()` 返回的值可能不是精确的字节数，而是“不透明”的位置标记，仅能用于后续的 `seek()` 操作。在二进制模式（`'rb'` 或 `'wb'`）下，返回值是精确的字节偏移量。

### 3.2 移动文件指针

`seek()` 方法用于将文件指针移动到指定的偏移量位置。

```python
file_object.seek(
    offset,  # 必填，偏移量（整数）
    whence=0  # 可选，参考点（默认为 0，即文件开头）
)
```

1. `offset`

   指定要移动的偏移量。

2. `whence`

   指定偏移量的参考基准。

   - `0`（默认值）：从文件开头计算偏移量。`offset` 必须为非负整数。
   - `1`：从当前文件指针位置计算偏移量。`offset` 可以为负数（向后回退）。
   - `2`：从文件末尾计算偏移量。`offset` 通常为负数或零（指向文件末尾）。

在文本模式下，`seek()` 仅允许 `whence=0` 且 `offset` 必须是 `tell()` 返回的值，或者 `offset=0`。其他形式的随机访问在文本模式下通常不被支持或行为未定义。二进制模式下支持所有形式的随机访问。

### 3.3 截断文件

`truncate()` 方法会将文件大小调整到当前文件指针的位置（或指定大小）。

```python
file_object.truncate(size=None)
```

1. `size`

   指定截断后的文件大小（字节数）。若省略，则截断至当前文件指针位置。

2. 作用

   如果当前指针位置小于原文件大小，超出部分的数据将被永久删除；如果当前指针位置大于原文件大小，文件将被扩展（扩展部分通常填充空字节 `\x00`）。

## 注意事项

- 文本模式与二进制模式的区别

  文件指针的精确控制强烈依赖于二进制模式。在文本模式下，由于 Python 会在读写时自动转换换行符（如将 `\r\n` 转换为 `\n`），导致逻辑字符数与物理字节数不一致。因此，`tell()` 和 `seek()` 的返回值在文本模式下是“不透明”的，仅保证 `f.seek(f.tell())` 能回到原位，不能进行任意字节级的随机访问。

- 写入时的指针行为

  在 `'w'` 或 `'a'` 模式下打开文件时，文件指针初始位置不同。`'w'` 模式会将指针置于文件开头（并清空文件），而 `'a'` 模式会将指针强制置于文件末尾。但在 `'a'` 模式下，操作系统通常会忽略 `seek()` 对写入位置的影响，确保数据始终追加到末尾。

- 缓冲区的影响

  文件操作通常带有缓冲区。`seek()` 操作会刷新缓冲区，确保指针移动前的数据被写入磁盘。在进行混合读写操作（`'r+'` 或 `'w+'` 模式）时，必须在读和写之间插入 `seek()` 或 `flush()` 调用，否则可能导致数据错乱。

- 跨平台兼容性

  在文本模式下处理换行符时，不同操作系统（Windows 使用 `\r\n`，Unix/Linux 使用 `\n`）会导致文件指针的偏移量计算出现差异。若需精确控制字节位置，务必使用二进制模式（`'rb'` 或 `'wb'`）。